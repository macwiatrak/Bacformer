def run():
    """Run the phenotypic traits prediction tutorial."""
    # 1. Load all the genomes with embedded genomes and phenotypic traits.
    # 2. train a Linear Regression model to predict the phenotypic traits from the genome embeddings.
    # 3. Evaluate the model performance.
    # 4. Plot the distribution of the predicted phenotypic traits.
    # 5. Show how one can use it on a completely new genome
    return


if __name__ == "__main__":
    run()
